[workspace]
resolver = "2"
exclude = ["angzarr-client-rust"]
members = [
    "proto",
    "examples-utils",
    "player/agg",
    "player/upc",
    "player/saga-table",
    "reservation/agg",
    "table/agg",
    "table/saga-hand",
    "table/saga-player",
    "hand/agg",
    "hand/upc",
    "hand/saga-table",
    "hand/saga-player",
    "hand-flow-oo",
    "tournament/agg",
    "prj-output",
    "pmg-hand-flow",
    "pmg-reservation",
    "tests",
]

[workspace.dependencies]
# Imported via git submodule
angzarr-client = { path = "angzarr-client-rust" }
examples-proto = { path = "proto" }
examples-utils = { path = "examples-utils" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
prost = "0.14"
prost-types = "0.14"
tonic = { version = "0.14", features = ["tls-ring", "transport"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
hex = "0.4"
sha2 = "0.10"
rand = "0.8"
chrono = "0.4"
uuid = { version = "1.0", features = ["v4"] }

# Container build profile - fast compilation, no incremental
[profile.container-dev]
inherits = "dev"
incremental = false

# Container release profile - optimized for size
[profile.production]
inherits = "release"
lto = true
opt-level = "z"
strip = true
