# Patch to add imagePullSecrets to all deployments
apiVersion: apps/v1
kind: Deployment
metadata:
  name: not-important
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
